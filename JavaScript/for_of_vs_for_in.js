let arr = ["Apple",10,true,900.9];

// fetching the values only from an array 
for(let a of arr){
    console.log(a);
}

// fetching values corresponding index or keys
for(let index in arr){
    console.log(index);
}